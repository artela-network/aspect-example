{
  "name": "salary",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy":"npm run build-all && node scripts/deploy.cjs",
    "test": "node tests",
    "asbuild:debug": "asc assembly/index.ts --target debug",
    "asbuild:release": "asc assembly/index.ts --target release",
    "asbuild": "npm run asbuild:debug && npm run asbuild:release",
    "start": "npx serve .",
    "build-contract": "asolc -o ./build/contract/ --via-ir --abi --bin --storage-layout ./contracts/*.sol  --overwrite",
    "build-all": "npm install && npm run build-contract && npm run asbuild:release"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@artela/aspect-libs":  "^0.0.20",
    "@artela/web3": "^1.9.14",
    "@artela/web3-atl-aspect": "^1.9.14",
    "@artela/web3-eth-contract": "^1.9.14",
    "@artela/web3-utils": "^1.9.8",
    "@assemblyscript/loader": "^0.27.5",
    "@openzeppelin/contracts": "^4.9.2",
    "as-proto": "^1.3.0"
  },
  "devDependencies": {
    "@artela/aspect-tool": "^0.0.36",
    "as-proto-gen": "^1.3.0",
    "assemblyscript": "^0.27.9"
  },
  "type": "module",
  "exports": {
    ".": {
      "import": "./build/release.js",
      "types": "./build/release.d.ts"
    }
  }
}
